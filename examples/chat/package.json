{
  "name": "chat",
  "version": "1.0.0",
  "description": "Example chat app with Elm Pusher",
  "main": "index.js",
  "scripts": {
    "start": "npm install && npm run netlify",
    "netlify": "netlify dev",
    "dev": "concurrently \"elm-spa watch\" \"vite\"",
    "build": "elm-spa gen && vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yarrow",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^6.0.2",
    "cypress": "^7.4.0",
    "elm-spa": "^6.0.4",
    "elm-test": "^0.19.1-revision7",
    "netlify-cli": "^3.31.16",
    "rollup-plugin-elm": "^3.0.0",
    "vite": "^2.2.2"
  },
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
